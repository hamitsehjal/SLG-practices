#define _CRT_SECURE_NO_WARNINGS

#include <stdio.h>

int main(void)
{
    int toonies, loonies, quarters, dimes, nickels, pennies;
    double balance = 239.74, remaining;

    printf("Your current balance is: $%.4lf\n", balance);

    printf("Coin      Quantity      Balance\n");
    printf("-------------------------------\n");
    
    
    // calculate toonies
    printf("Toonies   %d            );

    // calculate loonies
    printf("Loonies   %d            );

    // calculate quarters
    printf("Quarters  %d            );

    // calculate dimes
    printf("Dimes  %d            %.4lf);

    // calculate nickels
    printf("Nickels  %d            %.4lf);

    // calculate pennies
    printf("Pennies  %d            );
   
    return 0;
}