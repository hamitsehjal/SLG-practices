/**********************
* AUTHOR: Tarik Ozturk
* Date: 2022/10/09
* *********************/
#define _CRT_SECURE_NO_WARNINGS
#include <stdio.h>

#define MAX_NUM_EMP 10
#define MAX_SALARY 150000.0
int main(void) {
    double totalSalary, sumSalary = 0;
    int numEmp;

    // prompt user for total salary budget 
    // "Enter your total salary budget per month: $"
    // keep prompting if not between MAX_SALARY and 10000
    // display error if not in rage msgs ->
    // "Your total salary budget cannot be less than $10.000 per month\n" or "Your total salary budget cannot be more than $150.000 per month\n"

    // Write your code here
    

    // prompt user for max number of employees 
    // "Enter the number of employee you want to hire: "
    // keep prompting if not between MAX_NUM_EMP and 3
    // display error if not in rage msgs ->
    // "You should hire at least 3 people\n" or "You cannot hire more than 10 people\n"

    // Write your code here
    

    // Ask each employees salary
    // sum salary of each employee
    // if sum is more than your total salary budget -> prompt again
    // MUST USE -> array && flag
    
    return 0;
}
